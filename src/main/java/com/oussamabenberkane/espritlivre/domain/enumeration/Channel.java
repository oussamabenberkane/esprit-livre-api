package com.oussamabenberkane.espritlivre.domain.enumeration;

/**
 * The messaging channel a conversation belongs to.
 * Only WHATSAPP is active today; MESSENGER/INSTAGRAM are schema-ready for later.
 */
public enum Channel {
    WHATSAPP,
    MESSENGER,
    INSTAGRAM,
}
